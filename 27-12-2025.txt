#include <stdio.h>
int main() {
    int n;
    scanf("%d", &n);

    if (n % 5 == 0) {
        if (n % 10 == 0)
            printf("Divisible by 5 and 10");
        else
            printf("Divisible by 5 only");
    } else
        printf("Not divisible by 5");

    return 0;
}
